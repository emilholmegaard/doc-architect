# Example secrets file for Act
# Copy this to .secrets (gitignored) and fill in your values
# Usage: act -s GITHUB_TOKEN=your_token_here

# GitHub token for testing (use a PAT with minimal permissions)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here
